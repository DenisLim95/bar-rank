<!-- extends layout -->

block content
	h1 Upload beat page
	p This is the upload beat page bitches.

form(method='POST' action='/beat' enctype="multipart/form-data")
	div.form-group
		label(for='name') Name:
		input#name.form-control(type='file', name='file')
	div.form-group
		label(for='email') Email:
		input#email.form-control(type='submit', value='upload rendition')